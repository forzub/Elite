# Model Asset Editor v0.10.75 candidate

This candidate is intentionally one coherent iteration with two adjacent outputs: workflow-first editor presentation and a provisional motion-capable binary design target.

See `src/model_asset/MODEL_ASSET_V5_MOTION_CONTRACT.md` for the v5 package draft. The active production serializer remains v4 until the v5 writer/reader and production acceptance assets are implemented.

Runtime UI acceptance focus:
- active wizard tab is visibly brighter;
- each stage exposes a numbered action sequence;
- explanatory prose is moved behind `?` help while operational warnings remain visible;
- SEMANTICS TREE and STRUCTURAL GRAPH show different process sequences;
- existing authoring behavior/data contracts are unchanged.
